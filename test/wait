#!/bin/bash

set -ex

sleep 5

kubectl describe nodes

while true
do
	kubectl get pods --all-namespaces -o wide
	kubectl describe pods scylla-cluster-default-default-1-0 || echo 'scylla pod not yet present'
	sleep 5
done
